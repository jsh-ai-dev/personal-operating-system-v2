# Next.js 서버(Route Handler)가 Nest API로 프록시할 때만 사용합니다.
# 브라우저/클라이언트 번들에 넣지 마세요 — NEXT_PUBLIC_ 접두사 없음.
BACKEND_URL=http://localhost:3001

# mk1(Spring) 노트 API — `src/app/api/notes/...` BFF가 여기로 프록시합니다.
NOTES_SERVICE_URL=http://127.0.0.1:8080
